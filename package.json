{
  "name": "greeting",
  "version": "0.1.0",
  "description": "test",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "packageWin": "electron-packager . greeting --platform=win32 --arch=x64 --icon=hosts.ico --out=./dist --asar --app-version=0.1.0 --ignore=\"(dist|src|docs|.gitignore|LICENSE|README.md|webpack.config.js|node_modules)\""
  },
  "keywords": [],
  "author": "weilong",
  "license": "ISC",
  "dependencies": {
    "electron": "^1.8.4"
  },
  "devDependencies": {
    "electron-packager": "^12.0.1"
  }
}
